import transaction

class transaction_manager:
    def __init__():
        all_transactions = {}
        waits_for = {}
        wait_queue = {}

    def detect_deadlocks(waits_for_graph):
        # Detect deadlock
        return False

    def decipher_instruction(instr):
        # Decipher instruction
        return False

    def process_instruction(instr, site_manager):
        # Using site manager object, process instruction
        return False

    def abort_transaction(tid):
        # Abort transaction
        return False

    def create_transaction(tid):
        # Create transaction
        return False

    def commit_transaction(tid, site_manager):
        # Save uncommitted changes into sites
        return False

    def dump(site_manager):
        # Print status of variables in all sites
        return False

    def query_state(state_manager):
        # Print state of TM & SM
        return False